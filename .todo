# To implement (no specific order):
1. Hot-Reload
2. HTTP 2 & 3 support
3. Inbound streaming (request body processed in chunks as it arrives, instead of buffered whole)
4. HEAD auto-fallback to GET + wire-level body suppression: a HEAD to a GET-only route should return GET's headers with no body, not 404
5. onAbort-equivalent for request timeout

# To Fix:
1. Add write budget per connection (backend: epoll/io_uring/iocp/kqueue), so one connection can't hoard writing
2. When server reaches connection limit, drop random connections instead of the current policy
3. Template engine caching logic still needs a real pass
